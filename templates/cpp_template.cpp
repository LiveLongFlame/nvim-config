#include <bits/stdc++.h>
#define forn(i,n) for(int i =0; i < n;i++)
#define fora(i,a) for(int i =0; i < a.size();i++)
using namespace std;

int main(){
	ios::sync_with_stdio(false); cin.tie(nullptr);

	return 0;
}

